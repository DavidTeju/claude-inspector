export const base = '';
export const assets = '';

export function resolve(route: string, ..._params: unknown[]): string {
	return route;
}

export function resolveRoute(id: string, ..._params: unknown[]): string {
	return id;
}
